---
summary: "Agent tool surface for Verso (browser, canvas, nodes, message, cron) replacing legacy `verso-*` skills"
read_when:
  - Adding or modifying agent tools
  - Retiring or changing `verso-*` skills
---

# Tools (Verso)

Verso exposes **first-class agent tools** for browser, canvas, nodes, and cron.
These replace the old `verso-*` skills: the tools are typed, no shelling,
